<?xml version="1.0" encoding="utf-8"?>
<!-- Root FrameLayout allows badge overflow and transformer scaling -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clipChildren="false"
    android:clipToPadding="false">

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/cardCarousel"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginHorizontal="6dp"
        android:layout_marginVertical="10dp"
        app:cardCornerRadius="22dp"
        app:cardElevation="10dp"
        app:cardBackgroundColor="@color/mocha_card">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:gravity="center"
            android:paddingTop="16dp"
            android:paddingBottom="14dp"
            android:paddingHorizontal="10dp">

            <!-- Badge image — overflow upward for Sovereign+ -->
            <FrameLayout
                android:id="@+id/carouselBadgeContainer"
                android:layout_width="120dp"
                android:layout_height="120dp"
                android:clipChildren="false"
                android:clipToPadding="false">

                <ImageView
                    android:id="@+id/ivCarouselBadge"
                    android:layout_width="120dp"
                    android:layout_height="120dp"
                    android:layout_gravity="center"
                    android:scaleType="fitCenter"
                    android:adjustViewBounds="true" />

                <!-- Shine sweep overlay -->
                <View
                    android:id="@+id/viewCarouselShine"
                    android:layout_width="30dp"
                    android:layout_height="120dp"
                    android:background="@drawable/shine_sweep"
                    android:layout_gravity="center"
                    android:visibility="gone" />

                <!-- Lock overlay -->
                <View
                    android:id="@+id/viewCarouselLock"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:background="#80000000"
                    android:visibility="gone" />

                <ImageView
                    android:id="@+id/ivCarouselLockIcon"
                    android:layout_width="36dp"
                    android:layout_height="36dp"
                    android:layout_gravity="center"
                    android:src="@drawable/ic_lock"
                    android:visibility="gone" />

            </FrameLayout>

            <!-- CURRENT badge -->
            <TextView
                android:id="@+id/tvCarouselCurrentLabel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="● CURRENT RANK"
                android:textSize="9sp"
                android:textColor="@color/rank_gold"
                android:textStyle="bold"
                android:letterSpacing="0.1"
                android:layout_marginTop="4dp"
                android:visibility="gone" />

            <TextView
                android:id="@+id/tvCarouselName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="17sp"
                android:textColor="@color/text_primary"
                android:textStyle="bold"
                android:gravity="center"
                android:layout_marginTop="6dp" />

            <TextView
                android:id="@+id/tvCarouselQuote"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="11sp"
                android:textColor="@color/text_secondary"
                android:textStyle="italic"
                android:gravity="center"
                android:maxLines="2"
                android:ellipsize="end"
                android:layout_marginTop="4dp" />

            <TextView
                android:id="@+id/tvCarouselPoints"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="11sp"
                android:textColor="@color/rank_gold"
                android:textStyle="bold"
                android:layout_marginTop="6dp" />

        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

</FrameLayout>
